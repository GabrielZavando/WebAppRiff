---
import Layout from '@/layouts/Layout.astro';
---

<Layout title="Resultados de búsqueda — Riff">
  <main class="flex-1 flex items-center justify-center px-4 py-16">
    <div class="text-center max-w-xl">
      <h1 class="text-3xl font-bold text-secondary mb-4">Resultados de búsqueda</h1>
      <p class="text-gray-600 mb-6">
        <!-- TODO[product-catalog]: replace this placeholder with the real
             catalog page that consumes GET /api/v1/products?q=...&categoriaId=... .
             The search form (search-form change) navigates here with canonical
             query params; the catalog UI, filters, pagination and product cards
             will live in a future change. -->
        El catálogo de productos se mostrará aquí. Mientras tanto, vuelve al
        <a class="text-accent underline" href="/">inicio</a> para seguir
        explorando el sitio.
      </p>
    </div>
  </main>
</Layout>
