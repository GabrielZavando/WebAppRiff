## 1. Dependencias & Instalación

- [x] 1.1 Ejecutar `npm install` desde el workspace root (`/home/gabriel/Clientes/Riff/WebApp`) para materializar las deps del `apps/backend/package.json` vía hoisting del monorepo. Verificar que `apps/backend/node_modules/` ahora contiene `@nestjs/`, `firebase-admin/`, `jest/`, `ts-jest/`, `typescript/`, `eslint/`. Si `npm install` falla con `ERESOLVE` por conflictos de peer deps Angular/NestJS, reintentar con `npm install --legacy-peer-deps` y documentarlo en el `README.md`. — **DONE: `npm install` exit 0 sin ERESOLVE; deps hoisted al `node_modules` raíz (NestJS 10.4.22, typescript 5.5.4, ts-jest 29.4.12, eslint-plugin-sonarjs 4.2.0). `apps/backend/node_modules/` conserva solo el `@types` leftover (comportamiento normal de npm workspaces). 96 vulns `npm audit` (8 low/41 moderate/45 high/2 critical) pre-existentes, fuera de alcance.**
- [x] 1.2 Verificar `node --version` ≥ 22.12.0 (canónico según `package.json` raíz `engines.node`). — **DONE: node v22.23.2, npm 10.9.8.**
- [x] 1.3 Confirmar que `apps/backend/package.json` no requiere ediciones (todas las deps necesarias ya declaradas: NestJS 10, firebase-admin 12, jest, ts-jest, eslint, typescript, class-validator/transformer, throttler). El único añadido opcional es añadir el script `test:e2e` (ver 3.7); si se añade, queda en `apps/backend/package.json`. — **DONE: `package.json` backend sin cambios; se añade `test:e2e` en 3.3.**

## 2. Configs TypeScript & NestJS CLI

- [x] 2.1 Crear `apps/backend/tsconfig.json` con `strict: true`, `target: "ES2022"`, `module: "CommonJS"`, `experimentalDecorators: true`, `emitDecoratorMetadata: true`, `outDir: "./dist"`, `rootDir: "./src"`, `baseUrl: "./"`, `paths: { "@/*": ["src/*"] }`, `esModuleInterop: true`, `skipLibCheck: true`, `forceConsistentCasingInFileNames: true`, `declaration: true`, `sourceMap: true`. — **DONE. NOTA: se añadió `strictNullChecks`/`noImplicitAny`/etc. explícitos (redundantes con `strict` pero explícitos). Se OMITIÓ `incremental` (el NestJS estándar no lo usa; ver fix 7.1).**
- [x] 2.2 Crear `apps/backend/tsconfig.build.json` que `extends: "./tsconfig.json"` y añade `exclude: ["node_modules", "**/*.spec.ts", "**/*.e2e-spec.ts", "dist", "coverage"]`. — **DONE.**
- [x] 2.3 Crear `apps/backend/nest-cli.json` con `collection: "@nestjs/schematics"`, `sourceRoot: "src"`, `compilerOptions: { tsConfigPath: "tsconfig.build.json", deleteOutDir: true }`. — **DONE.**

## 3. Configs Jest

- [x] 3.1 Crear `apps/backend/jest.config.js` con `preset: "ts-jest"`, `testEnvironment: "node"`, `rootDir: "."`, `roots: ["<rootDir>/src"]`, `moduleFileExtensions: ["js", "json", "ts"]`, `moduleNameMapper: { "^@/(.*)$": "<rootDir>/src/$1" }`, `testRegex: ".*\\.spec\\.ts$"`, `transform: { "^.+\\.(t|j)s$": ["ts-jest", { tsconfig: "tsconfig.json" }] }`, `coverageDirectory: "./coverage"`, `collectCoverageFrom: ["src/**/*.(t|j)s", "!src/main.ts"]`, `coverageThreshold: { global: { statements: 90, branches: 90, functions: 90, lines: 90 } }`. — **DONE.**
- [x] 3.2 Crear `apps/backend/jest-e2e.json` con `preset: "ts-jest"`, `testEnvironment: "node"`, `rootDir: "."`, `roots: ["<rootDir>/test"]` (o `src` si e2e vive junto), `testRegex: ".*\\.e2e-spec\\.ts$"`, `transform: { "^.+\\.(t|j)s$": ["ts-jest", { tsconfig: "tsconfig.json" }] }`. NOTA: en este change no hay archivos e2e todavía; la config queda lista para cuando se añadan en `backend-commons`. — **DONE.**
- [x] 3.3 (Opcional) Añadir `apps/backend/package.json` script: `"test:e2e": "jest --config jest-e2e.json"`. Si se añade, el script no debe romper `npm test` (que sigue usando `jest.config.js`). — **DONE: script `test:e2e` añadido.**
- [x] 3.4 Verificar `npm test --workspace=@riff/backend` corre sin specs y sale 0 (sin archivos de test todavía, Jest pasa con "No tests found" pero exit 0 si `passWithNoTests: true` está seteado; añadir `passWithNoTests: true` a `jest.config.js`). — **DONE: `passWithNoTests: true` añadido a ambos configs. La verificación inicial falló porque `roots` apuntaba a `src/` inexistente; resuelto al crear `src/` en G5.**

## 4. Lint SOLID & dependency-cruiser

- [x] 4.1 Copiar `templates/ci/eslintrc.backend.js` a `apps/backend/.eslintrc.cjs` y adaptar (paths: `src/**/*.ts`, parserOptions con `tsconfigRootDir` apuntando a `apps/backend`, plugins `@typescript-eslint`, `sonarjs` si el template los usa). Verificar que `max-lines: ["error", 300]` y `complexity: ["error", 10]` quedan presentes. Si el template referencia `eslint-plugin-sonarjs` que NO está en `package.json`, omitir esa regla (no añadir deps) o documentar que se aplaza a `backend-commons`. — **DONE: adaptado manteniendo solo umbrales core (`max-lines` 300, `complexity` 10). `eslint-plugin-sonarjs` (declarado solo en apps/web, hoisted) y `eslint-plugin-import` (NO instalado) se difieren a `backend-commons` por decisión de diseño "no añadir deps". Documentado en el header del `.eslintrc.cjs`.**
- [x] 4.2 Copiar `templates/ci/.dependency-cruiser.js` a `apps/backend/.dependency-cruiser.js`. En este change no hay `domain/` ni `infrastructure/`, por lo que la regla `no-infra-from-domain` no se dispara; queda como guard preventivo. — **DONE: copiado verbatim.**
- [x] 4.3 Crear `apps/backend/.eslintignore` con `dist/`, `coverage/`, `node_modules/` si el `.eslintrc.cjs` no los excluye por default. — **DONE: creado con dist/, coverage/, node_modules/.**
- [x] 4.4 Ejecutar `npm run lint --workspace=@riff/backend` → verde (no hay código todavía, pero el lint no debe crashear). — **DONE: exit 0, sin errores ni warnings.**

## 5. Source — Bootstrap & /health (TDD)

- [x] 5.1 Escribir `apps/backend/src/app.service.spec.ts` que falle primero: instancia `AppService` y `expect(getStatus()).toBe("ok")`. Verificar `npm test` → RED.
- [x] 5.2 Implementar `apps/backend/src/app.service.ts` con `@Injectable()` `AppService` y método `getStatus(): string` retornando `"ok"`. Verificar `npm test` → GREEN para AppService.
- [x] 5.3 Escribir `apps/backend/src/app.controller.spec.ts` que falle primero: instancia `AppController` (con `AppService` mocked o real vía `Test.createTestingModule`), invoca `getHealth()` y `expect(result).toEqual({ status: "ok" })`. Verificar RED.
- [x] 5.4 Implementar `apps/backend/src/app.controller.ts` con `@Controller()` `AppController` constructor-inyectando `AppService`, método `@Get("health") getHealth()` que retorna `{ status: this.appService.getStatus() }`. Verificar GREEN.
- [x] 5.5 Implementar `apps/backend/src/app.module.ts` con `@Module({ controllers: [AppController], providers: [AppService] })` `AppModule`.
- [x] 5.6 Implementar `apps/backend/src/main.ts` con bootstrap: `NestFactory.create(AppModule)` → `app.listen(process.env.PORT ?? 3000)` → log con `Logger` informando el puerto. Usar `??` (nullish coalescing) para distinguir `0` válido de ausente. — **DONE (CORREGIDO en adversarial review): la resolución del puerto se extrajo a `src/config/port.ts` (`resolvePort`), función pura testeable que cubre undefined/inválido/negativo/empty → 3000 y `"0"` → ephemeral. `main.ts` usa `resolvePort(process.env.PORT)` y añadió `bootstrap().catch(...)` con `process.exit(1)` (el `??` simple no cubría el caso "invalid" que sí exige el spec).**
- [x] 5.7 Verificar `npm test --workspace=@riff/backend -- --coverage` → verde Y coverage global ≥ 90% (cubrir AppController + AppService; `main.ts` está excluido de coverage por `collectCoverageFrom`). — **DONE: 100% statements/branches/functions/lines. Se añadió `app.module.spec.ts` (test de wiring del decorador `@Module`) porque `app.module.ts` se contaba como no cubierto y bajaba el global a 68%.**
- [x] 5.8 Verificar `npm run typecheck --workspace=@riff/backend` → exit 0. — **DONE.**

## 6. Integración manual & smoke

- [x] 6.1 Ejecutar `npm run start --workspace=@riff/backend` en background. Verificar que levanta en puerto 3000 (ver log de startup). — **DONE: `nest start --watch` levantó, log "Application is running on port 3000", ruta `/health` mapeada.**
- [x] 6.2 Hacer `curl -s http://localhost:3000/health` → `{"status":"ok"}` con HTTP 200. Verificar manualmente (no se exige e2e automático en este change; el e2e HTTP real con supertest entra en `backend-commons`). — **DONE: HTTP 200 + `{"status":"ok"}`.**
- [x] 6.3 Verificar que `GET /health` NO requiere `Authorization` header (curl sin header → 200). — **DONE: curl sin header → HTTP 200.**
- [x] 6.4 Hacer `curl -s http://localhost:3000/api/v1/health` (si existe la ruta) → debe ser 404 (este change no setea prefijo `/api/v1`). — **DONE: HTTP 404.**
- [x] 6.5 Detener el proceso (`kill` por PID o Ctrl-C). — **DONE: procesos `dist/main` terminados; curl posterior → conexión rechazada.**

## 7. Build & Dockerfile

- [x] 7.1 Ejecutar `npm run build --workspace=@riff/backend` → exit 0. Verificar que `apps/backend/dist/main.js` existe y que NO existe `apps/backend/dist/**/*.spec.js` ni `*.e2e-spec.js`. — **DONE: `dist/main.js` + 4 archivos `.js`/.d.ts/.map`; sin specs. FIX de apply: se removió `incremental: true` del `tsconfig.json` (lo añadí de más) porque combinado con `deleteOutDir: true` hacía que tsc saltara la re-emisión usando el cache `.tsbuildinfo` del watch previo, dejando `dist/` vacío. El tsconfig NestJS estándar no usa incremental. Se borraron los `*.tsbuildinfo` residuales.**
- [x] 7.2 Crear `apps/backend/Dockerfile` multi-stage: stage `build` (base `node:22`, copia `package.json` raíz + `package-lock.json` + `apps/backend/package.json`, `npm ci --workspace=@riff/backend`, copia `apps/backend/src/` + configs `tsconfig.json`/`tsconfig.build.json`/`nest-cli.json`, `npm run build --workspace=@riff/backend`), stage `runtime` (base `node:22-slim`, copia `apps/backend/dist/` + `apps/backend/node_modules/` desde build, `EXPOSE 3000`, `CMD ["node", "dist/main.js"]`). — **DONE: Dockerfile creado con contexto = raíz del monorepo (no apps/backend) para que `npm ci --workspace` resuelva; copia los 3 package.json de workspaces; `npm prune --omit=dev` para imagen slim.**
- [x] 7.3 Crear `apps/backend/.dockerignore` excluyendo `node_modules/`, `coverage/`, `.env`, `*.local`, `dist/`. — **DONE (CORREGIDO en adversarial review): el `.dockerignore` vive en la RAÍZ del monorepo (`.dockerignore`), no en `apps/backend/`, porque el contexto de build del Dockerfile es la raíz. Docker solo lee `.dockerignore` desde la raíz del contexto. Se creó `/home/gabriel/Clientes/Riff/WebApp/.dockerignore` (node_modules/, dist/, coverage/, .git/, .env*, *.local, *.tsbuildinfo, artefactos de web/admin) y se eliminó el `apps/backend/.dockerignore` muerto.**
- [x] 7.4 Verificar `docker build -t riff-backend-scaffold ./apps/backend` (o contexto raíz monorepo según Dockerfile) → exit 0. — **DONE: `docker build -f apps/backend/Dockerfile -t riff-backend-scaffold .` → exit 0, imagen creada.**
- [x] 7.5 Verificar con `docker run -d -p 3001:3000 riff-backend-scaffold` → `curl http://localhost:3001/health` → `{"status":"ok"}` 200. — **DONE: container respondió `{"status":"ok"}` HTTP 200 en puerto 3001; container e imagen se limpiaron tras el smoke.**

## 8. Misc (.env.example, .gitignore, README)

- [x] 8.1 Crear `apps/backend/.env.example` con `PORT=3000` (comentado que variables Firebase/CORS/Throttler entran en changes posteriores). — **DONE.**
- [x] 8.2 Crear `apps/backend/.gitignore` con `dist/`, `coverage/`, `.env`, `*.local`, `node_modules/` (aunque `node_modules/` ya está en el raíz `.gitignore`, idempotente). — **DONE.**
- [x] 8.3 Crear `apps/backend/README.md` con: descripción 1-línea ("NestJS BFF para Riff Catálogo Digital"), requisitos (Node ≥ 22.12.0, npm 10, Firebase project creds en changes posteriores), setup 3 pasos (`npm install` desde raíz del monorepo, copiar `.env.example` a `.env`, `npm run start:dev --workspace=apps/backend`), variables env (tabla con `PORT` y notas de las que vendrán), comandos clave (`dev`, `test`, `lint`, `build`, `typecheck`). — **DONE.**
- [x] 8.4 Confirmar que el `.env.example` raíz (`/home/gabriel/Clientes/Riff/WebApp/.env.example`) NO se toca (rewrite entra en `backend-firebase-config`). — **DONE: no aparece en `git status`; sin cambios.**

## 9. Validación & SDD

- [x] 9.1 `npm run lint --workspace=@riff/backend` → exit 0 (incluye los umbrales SOLID max-lines 300 / complexity 10). — **DONE: exit 0.**
- [x] 9.2 `npm run typecheck --workspace=@riff/backend` → exit 0. — **DONE.**
- [x] 9.3 `npm test --workspace=@riff/backend -- --coverage` → all pass + coverage ≥ 90% en todos los umbrales globales (statements/branches/functions/lines). — **DONE: 9 tests, 100% coverage.**
- [x] 9.4 `npm run build --workspace=@riff/backend` → exit 0 + `dist/main.js` producido. — **DONE.**
- [x] 9.5 `make solid-lint` desde raíz del monorepo → el target ahora ejecuta su rama backend (antes inactiva por ausencia de `src/`) y sale verde. Verificar que no introdujo regresiones en web/admin. — **DONE CON HALLAZGO (pre-existente del template, no regresión de este change): `make solid-lint` desde raíz es un no-op (exit 0, imprime "PASS") porque sus ramas verifican `[ -d src ]` relativo al CWD raíz, que en un monorepo NO existe (el código vive en `apps/backend/src`). Además: (1) `templates/ci/eslintrc.backend.js` está roto contra el `eslint-plugin-sonarjs@4.2.0` instalado (config inválida `plugin:sonarjs/recommended`); (2) `dependency-cruiser` y `madge` NO están instalados. El lint SOLID real del backend se aplica vía `npm run lint --workspace=@riff/backend` (`.eslintrc.cjs` local). Hacer que `solid-lint` recorra los workspaces del monorepo es una mejora separada (change de tooling), fuera del alcance de este scaffold.**
- [x] 9.6 `npm run start --workspace=@riff/backend` (manual) + `curl http://localhost:3000/health` → `{"status":"ok"}` 200, sin `Authorization`. — **DONE (ver G6): puerto 3000 y puerto custom 4000 ambos verificados.**
- [x] 9.7 `openspec validate backend-scaffold` → valid. — **DONE.**
- [x] 9.8 `openspec status --change backend-scaffold --json` → todos los artefactos done (`isComplete: true` o `applyRequires` cubierto). — **DONE: 4/4 artefactos complete.**
- [x] 9.9 Re-ejecutar los escenarios Gherkin del spec `backend-runtime` contra la implementación: (a) backend arranca en puerto default y custom, (b) `GET /health` → 200 `{ "status": "ok" }` sin auth, (c) typecheck strict pasa, (d) build excluye specs de `dist/`, (e) lint verde, (f) coverage ≥ 90%, (g) AppService.getAppController cubiertos por specs, (h) Docker build + health del container, (i) `.env.example` documenta `PORT` y `.gitignore` excluye `.env`. — **DONE: todos verificados; escenario (a) incluye puerto default 3000 y custom 4000; escenario de puerto inválido añadido en adversarial review y cubierto por `resolvePort` (test).**
- [x] 9.10 Adversarial review (skills `code-auditing` con lente Architect/SOLID): zero critical warnings en `apps/backend/src/`, `.eslintrc.cjs`, `tsconfig.json`, `jest.config.js`, `Dockerfile`. Documentar hallazgos no bloqueantes (vulns `npm audit` pre-existentes quedan explicitados fuera de alcance). — **DONE: 0 CRITICAL. 3 MAJOR corregidos (validación PORT → `resolvePort`; `.dockerignore` movido a la raíz del monorepo; `USER node` en Dockerfile) + 4 MINOR (bootstrap `.catch()`, `app.module.spec.ts` con `Test.createTestingModule`, `@types/node` ^20 → diferido, bookkeeping de tasks). Vulns `npm audit` (96: 8 low/41 moderate/45 high/2 critical) pre-existentes, fuera de alcance.**
