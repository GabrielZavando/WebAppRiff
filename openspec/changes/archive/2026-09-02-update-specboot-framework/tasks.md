# Tasks: update-specboot-framework

## 1. Prerequisites

- [x] 1.1 Verify the published package version with `npm view @gabrielzavando/specboot version` and record it (used for `.specboot.json` `frameworkVersion`)
- [x] 1.2 Create branch `feature/update-specboot-framework` from `main` and confirm working tree is clean

## 2. Package consumption (specboot-package-consumption)

- [x] 2.1 Create root `.npmrc` containing only `@gabrielzavando:registry=https://npm.pkg.github.com` (no token) and verify it is committed
- [x] 2.2 Run `npm install --save-dev @gabrielzavando/specboot`; verify root `package.json` devDependency, updated `package-lock.json`, and `node_modules/@gabrielzavando/specboot/` exist
- [x] 2.3 Create root `.specboot.json` with `frameworkVersion` = version from 1.1, `name` `riff-catalogo-digital`, `description`, `services: ["."]`, `stack: ["node"]`; verify with `node -e "require('./.specboot.json')"`

## 3. Framework sync via specboot update (specboot-framework-sync)

- [x] 3.1 Run `bash node_modules/@gabrielzavando/specboot/specboot.sh update --dry-run` and review the 17 replacement items (no changes applied)
- [x] 3.2 Run `bash node_modules/@gabrielzavando/specboot/specboot.sh update`; verify backup created at `.specboot-backup-<ts>`, no prompt shown, and post-update validation output
- [x] 3.3 Restore project `.github/workflows/ci.yml` from the update backup and apply the auth patch: workflow-level `permissions: { contents: read, packages: read }`, `env: NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`, and `registry-url: https://npm.pkg.github.com` in the 5 `setup-node` steps of jobs that run `make install` (lint, test, build, audit, solid-lint); validate YAML parses
- [x] 3.4 Delete `.github/workflows/release.yml`; keep the framework's gated `deploy.yml` unchanged
- [x] 3.5 Delete deprecated template tooling: root `update.sh` and `tests/check-refs-test.sh`, `tests/solid-templates-test.sh`, `tests/update-test.sh`
- [x] 3.6 Verify `.gitignore` contains `.specboot-backup-*` (added by update) and that `.commitlintrc.json`, `.opencode/package.json`, and `.opencode/skills/openspec-*` are unmodified

## 4. Canonical docs migration (specboot-docs-canonical)

- [x] 4.1 `git mv docs/api-spec.yml docs/api/api-spec.yml` and `git mv docs/data-model.md docs/data-model/data-model.md`
- [x] 4.2 Extract Riff context from current `docs/base-standards.md` §8 into new `docs/project/stack.md`, `docs/project/domain.md`, `docs/project/client.md` (no information loss); then replace `docs/base-standards.md` with the framework-canonical version from `node_modules/@gabrielzavando/specboot/docs/base-standards.md`
- [x] 4.3 Update path references `docs/api-spec.yml` → `docs/api/api-spec.yml` and `docs/data-model.md` → `docs/data-model/data-model.md` in `docs/documentation-standards.md`, `docs/seed-catalog.md`, `README.md`, `apps/backend/README.md`, and `openspec/specs/**/*.md` (living specs only); grep to confirm zero stale occurrences
- [x] 4.4 Verify `openspec/changes/archive/**`, `docs/design/**`, `apps/*/src`, and the content of backend/frontend/ci/deploy standards docs are untouched

## 5. Commit message enforcement (commit-msg-enforcement)

- [x] 5.1 Run `npm pkg set scripts.prepare="husky"` and `npx husky init`; verify `.husky/` exists and `prepare` script is in root `package.json`
- [x] 5.2 Create `.husky/commit-msg` with `npx --no -- commitlint --edit $1`; remove/empty the sample `.husky/pre-commit` generated by init; ensure `.husky/_` is gitignored

## 6. Validation (acceptance criteria)

- [x] 6.1 Run `bash specboot.sh --init` and `bash specboot.sh --ci` — both exit 0 with no husky warnings
- [x] 6.2 Run `bash check-refs.sh` — exit 0
- [x] 6.3 Run `make install` (root `npm ci` resolves the private package), then `make refs`, `make lint`, `make test`, `make build`, `make audit`, `make solid-lint`, `make commitlint` — all succeed (`make solid-lint` may run advisory eslint; must not abort)
- [x] 6.4 Validate `.husky/commit-msg` locally: a commit with a non-conventional message is rejected and one with `feat(web): test message` passes (amend/reset afterwards)
- [x] 6.5 Review `git diff` scope: only framework-owned replacements, `git mv` docs, ci.yml auth patch, reference updates, deletions from 3.4/3.5, `.specboot.json`, `.npmrc`, husky files — and zero changes under `apps/*/src`

## 7. Runbook and close-out

- [x] 7.1 Append the post-update runbook to this change (restore `ci.yml` from backup, delete `release.yml`, re-apply auth patch if backup predates it) so it ships with the archived change documentation
- [x] 7.2 Confirm CI prerequisites are actionable: package grant (GitHub Package settings → Manage Actions access → repo WebApp, read) and commit the auth-patched `ci.yml`
