# Tasks — ui-refactor

> **Objetivo**: pulir la UI de los 5 componentes base del sitio público (`TopHeader`, `Header`, `HeroBanner`, `PanelHome`, `SearchForm`) en tres ejes: (1) iconos → **Lucide** (set único, uniformando también los logos sociales); (2) tipografía → aplicar la escala completa de la guía (Montserrat/Open Sans) que hoy NO se aplica (`font-heading` ausente en títulos/botones/nav); (3) flat design estricto con **radio 0** + limpieza de tokens residuales (`gray-*` → tokens) + sin `shadow` en componentes base. Solo `apps/web` (admin sincroniza `globals.css` pero no crea componentes). Sin líneas decorativas nuevas (futuro change).

## 1. Setup de dependencias

- [x] 1.1 Test (RED): actualizar `apps/web/src/config/__tests__/package.test.ts` — cambiar `REQUIRED_DEPS` para exigir `@iconify-json/lucide` en vez de `@iconify-json/material-symbols`. Añadir nuevo test que afirme que `@iconify-json/material-symbols` y `@iconify-json/logos` NO están declarados en `apps/web/package.json`. El test debe fallar (lucide no está aún, los otros sí).
- [x] 1.2 Implementar (GREEN): en `apps/web/package.json`, agregar `@iconify-json/lucide` (`^1.x`) a `dependencies`; remover `@iconify-json/material-symbols` y `@iconify-json/logos`. Ejecutar `npm install` en `apps/web`. El test 1.1 pasa.

## 2. Token `--radius: 0` + sync admin

- [x] 2.1 Test (RED): actualizar `apps/web/src/styles/__tests__/tokens.test.ts` — el test "Task 2.9 — Radio y sombras / declares --radius: 2px" cambia su assert a `expect(theme).toMatch(/--radius\s*:\s*0(px)?/)` (radio 0). Añadir nuevo test que afirme que ningún componente base aplica utility `rounded*`. El test debe fallar (`--radius` aún es `2px`).
- [x] 2.2 Implementar (GREEN): en `apps/web/src/styles/globals.css` y `apps/admin/src/styles/globals.css`, cambiar `--radius: 2px` → `--radius: 0` (ambos para mantener el sync test). El test 2.1 pasa; el sync test (`apps/admin/src/styles/__tests__/sync.test.ts`) sigue verde por construcción.

## 3. Iconos → Lucide

- [x] 3.1 Test (RED→GREEN): no hay nuevos tests de asserts de nombre-icono (los tests existentes verifican `aria-label` y `<svg>`, no el nombre del set). Solo se regeneran snapshots. Apuntar aquí el RED es opcional — el red está cubierto por el test del catálogo (task 6.2). Proceder a implementar.
- [x] 3.2 Implementar `TopHeader.astro`: en el `socialIconMap`, cambiar los valores `logos:*` → `lucide:*` (`Facebook: 'lucide:facebook'`, `X: 'lucide:twitter'`, `Instagram: 'lucide:instagram'`, `LinkedIn: 'lucide:linkedin'`); cambiar el icono del teléfono `material-symbols:contact-phone-outline` → `lucide:phone`. Los `aria-label` y el layout NO cambian.
- [x] 3.3 Implementar `Header.astro`: cambiar `material-symbols:menu-outline` → `lucide:menu` y `material-symbols:close-outline` → `lucide:x`. El script inline que conmuta `svg` entre los dos iconos sigue siendo válido (astro-icon genera `<svg>` en ambos casos).
- [x] 3.4 Regenerar snapshots: `npx vitest run TopHeader Header -u` desde `apps/web`. Revisar el diff manualmente (deben cambiar solo los `name` attribute de los `<svg>` y los paths internos).
- [x] 3.5 Verificar tests existentes: `TopHeader.test.ts` (tel: link contiene `<svg>`, 4 aria-labels, 3 dividers `border-white/20`) y `Header.test.ts` (toggle con dos `<Icon>`, aria-expanded) siguen verdes.

## 4. Tipografía — escala completa de la guía (familia + peso + tamaño)

- [x] 4.1 Test (RED): en `apps/web/src/components/__tests__/Header.test.ts`, actualizar el test "renders the CTA with label, href and orange bold styling" — la aserción `expect(html).toContain('font-bold')` cambia a `expect(html).toContain('font-semibold')`, y se añade `expect(html).toContain('font-heading')`. Añadir test que afirme que los items de nav llevan `font-heading` y `font-semibold` (Montserrat 600). El test debe fallar.
- [x] 4.2 Implementar `Header.astro`: nav items `px-3 py-2 text-sm font-medium uppercase tracking-wide` → `px-3 py-2 text-xs font-heading font-semibold uppercase tracking-wide`; CTA `font-bold uppercase text-sm` → `font-heading font-semibold uppercase text-xs tracking-wide`; mobile nav links añaden `font-heading font-semibold`. El test 4.1 pasa.
- [x] 4.3 Implementar `HeroBanner.astro`: h1 añade `font-heading` (conserva `font-bold text-4xl md:text-6xl`); h2 añade `font-heading font-semibold` (conserva `text-xl md:text-2xl`); CTAs `px-8 py-3 font-bold uppercase tracking-wide text-sm` → `px-8 py-3 font-heading font-semibold uppercase tracking-wide text-xs`. No hay tests de aserción nueva (los tests existentes validan `font-bold` solo en h1; en CTAs NO asertan peso). Regenerar snapshot.
- [x] 4.4 Implementar `PanelHome.astro`: eyebrow `text-sm font-bold uppercase tracking-wider` → `text-xs font-heading font-semibold uppercase tracking-wider`; h2 headline añade `font-heading` (conserva `font-bold text-2xl md:text-3xl lg:text-4xl`); CTA `font-bold uppercase text-sm` → `font-heading font-semibold uppercase text-xs tracking-wide`; stat-values añaden `font-heading` (conserva `font-bold text-3xl md:text-4xl text-secondary`). El test 2.13 (`font-bold`+`text-secondary`) sigue verde. Regenerar snapshot.
- [x] 4.5 Implementar `SearchForm.astro`: submit button añade `font-heading font-semibold` (conserva `text-xs uppercase tracking-wide`); select e input conservan `text-sm` (decisión: no bajar a `text-[13.5px]` arbitrario, ver design.md trade-off). Regenerar snapshot.
- [x] 4.6 Regenerar snapshots restantes: `npx vitest run HeroBanner PanelHome SearchForm -u` desde `apps/web`. Revisar diffs.

## 5. Flat design + radio 0 + limpieza de tokens residuales

- [x] 5.1 Test (RED): en `apps/web/src/components/__tests__/SearchForm.test.ts`, actualizar los tests del wrapper del transparent mode: cambiar `expect(html).toContain('border-gray-200')` → `expect(html).toContain('border-border')`, y `wrapperClass).not.toContain('border-gray-200')` se mantiene (sigue sin gray). Añadir test que afirme que select, input y button NO llevan `rounded` (regex `/rounded/` ausente en cada control). El test debe fallar (hay `rounded` y `border-gray-200`).
- [x] 5.2 Implementar `SearchForm.astro`: wrapper `bg-white border-b border-gray-200` → `bg-white border-b border-border`; select/input `text-gray-700` → `text-text-2`; eliminar `rounded` de select, input y button. El test 5.1 pasa.
- [x] 5.3 Test (RED): en `apps/web/src/components/__tests__/PanelHome.test.ts`, actualizar el test "each stat label is rendered as a <p>... with uppercase and a gray text color class (task 2.14)" — cambiar la regex `/text-gray-/` → `text-text-2` (assert `p.includes('text-text-2')`). El test debe fallar (hay `text-gray-600`). Añadir test que afirme que el CTA del panel NO lleva `rounded`.
- [x] 5.4 Implementar `PanelHome.astro`: stat labels `text-gray-600` → `text-text-2`; eliminar `rounded` del CTA; stat labels `text-xs md:text-sm` se mantienen (la guía no obliga cambiar tamaño de labels, solo el token de color). El test 5.3 pasa.
- [x] 5.5 Implementar `Header.astro`: eliminar `rounded` del CTA y de los mobile nav links; eliminar `shadow-sm` del CTA. No hay tests RED específicos (los tests existentes no asertan `rounded` ni `shadow-sm`); la validación la cubre el test del catálogo (task 6.2) y el snapshot. Regenerar snapshot. *(Adelantado en 4.2.)*
- [x] 5.6 Implementar `HeroBanner.astro`: eliminar `rounded` de los CTAs (primary y secondary). Regenerar snapshot. *(Adelantado en 4.3.)*
- [x] 5.7 Regenerar snapshots finales: `npx vitest run -u` desde `apps/web` (todos los componentes). Revisar diffs manuales — los únicos cambios deben ser: `name` de iconos Lucide, `font-heading`/`font-semibold`/`text-xs`, ausencia de `rounded`, ausencia de `border-gray-200`/`text-gray-*`, `--radius: 0`.

## 6. Documentación

- [x] 6.1 Actualizar `docs/design/style-guide/README.md`: en "Catálogo de Iconos", reemplazar la tabla Material Symbols/Logos por la tabla **Lucide** (21 iconos mínimos + el mapeo de los 5 componentes: `phone`→`lucide:phone`, `menu`→`lucide:menu`, `close`→`lucide:x`, `facebook`→`lucide:facebook`, `x`→`lucide:twitter`, `instagram`→`lucide:instagram`, `linkedin`→`lucide:linkedin`). Actualizar la nota: set único autorizado `lucide`; `material-symbols` y `logos` quedan obsoletos. En "Radio y Sombras", cambiar `--radius: 2px` → `--radius: 0` y la nota "Flat design principle" para explicitar radio 0 (ángulos rectos).
- [x] 6.2 Test (RED→GREEN) del catálogo: en `apps/web/src/styles/__tests__/tokens.test.ts` (o un nuevo test dedicado), añadir un test que lea `docs/design/style-guide/README.md` y afirme que contiene la tabla Lucide y NO menciona `material-symbols:`/`logos:` como sets autorizados; y un test que lea los componentes y afirme que ningún `<Icon name="...">` usa prefijo distinto de `lucide:`. El test debe fallar antes de completar 6.1 y pasar después.
- [x] 6.3 Actualizar `docs/frontend-standards.md`: en la sección "Design Tokens", cambiar la mención "1 radio `2px`" → "radio `0` (flat design estricto, ángulos rectos)"; y la convención de iconos: "Material Symbols Outline para UI, Logos para marcas" → "Lucide (set único, outline stroke 2px) para UI y marcas sociales".

## 7. Verificación final

- [x] 7.1 `cd apps/web && npm run typecheck` — sin errores (`astro check && tsc --noEmit`).
- [x] 7.2 `cd apps/web && npm run lint` — sin errores nuevos. *(Se removió el import `readdirSync` sin uso en tokens.test.ts, error pre-existente que bloqueaba el lint.)*
- [x] 7.3 `cd apps/web && npm run test` — suite completa verde (todos los .test.ts + snapshots regenerados). Confirmar que no queda ningún `border-gray-200`, `text-gray-700`, `text-gray-600`, `material-symbols:`, `logos:`, `rounded` (sin variantes) ni `shadow-sm` en los 5 componentes.
- [x] 7.4 `cd apps/web && npm run build` (SSG) — sin warnings nuevos; `dist/index.html` y `dist/productos/index.html` generados.
- [x] 7.5 `cd apps/web && npm run test:smoke` (Playwright e2e) — todos los specs (`top-header`, `site-header`, `search-form`, `hero-banner`, `panel-home`) verdes, incl. los checks WCAG AA de contraste (el cambio `text-gray-600`→`text-text-2` en stat labels mantiene >5:1 sobre blanco; el panel teal `#41B3C4` sigue validando). *(53 tests verdes tras el fix 7.5a.)*
- [x] 7.5a Fix e2e pre-existentes (descubiertos al ejecutar 7.5; NO causados por ui-refactor — probado con baseline en HEAD vía `git stash`): (1) `hero-banner.spec.ts` y `panel-home.spec.ts` usan el selector stale `section.bg-secondary` del hero, roto por el change archivado `hero-fullbleed-overlay` (commit `3b78f52` cambió la sección a `<section class="relative">` sin actualizar los e2e) → reemplazar por `section.relative:not(.z-10)` (único match en la home: el hero es `section.relative`; el PanelHome es `section.relative.z-10`). (2) El mismo change movió el `<Picture>` del hero al `Layout.astro` (fondo full-bleed a nivel `<body>`), por lo que el test `real-site-images` debe apuntar a `body > picture` en vez de `${HERO_SECTION_SELECTOR} picture`. (3) `search-form.spec.ts:227` falla por strict-mode de `page.locator('header')` (la Dev Toolbar de Astro 7 inyecta `<header>` extra en `astro preview`) → usar `page.getByRole('banner')` (único elemento con rol banner; los headers de la toolbar no lo tienen). Re-ejecutar 7.5 hasta verde.
- [x] 7.6 Verificación visual manual (humana): abrir `/` y `/productos` en 4 viewports (375, 768, 1280, 1920). Confirmar: ángulos rectos en todos los componentes, iconos Lucide cargados (no vacíos), tipografía Montserrat visible en títulos/botones/nav, separadores sociales del TopHeader correctos. Documentar observaciones o abrir follow-up si algo no calza (no bloquea el archive si tests/e2e verdes).
