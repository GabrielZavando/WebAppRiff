## 1. Footer types — promote `scheduleTitle` and widen `hours`

TDD: update the type tests first (red), then update the types (green).

- [x] 1.1 Update `apps/web/src/lib/types/__tests__/footer.test.ts` (RED) — add assertions: `SiteFooterProps` includes a `readonly scheduleTitle: string`; `FooterScheduleEntry.hours` is `readonly string[]` (assert a `string[]` literal is assignable and a single `string` is NOT assignable); `SITE_FOOTER_CONTENT` is still assignable to the updated `SiteFooterProps`. Run the test → it fails.
- [x] 1.2 Update `apps/web/src/lib/types/footer.ts` (GREEN) — add `readonly scheduleTitle: string` to `SiteFooterProps`; change `FooterScheduleEntry.hours` from `string` to `readonly string[]`. Update the type-level JSDoc. Re-run the type test → green.

## 2. Footer config — new `scheduleTitle`, split-shift `schedule`, new `SERVICIOS` links

TDD: update the config test first (red), then update the config (green).

- [x] 2.1 Update `apps/web/src/lib/config/__tests__/footer.test.ts` (RED) — replace the schedule assertions: expect `siteTitle === 'Horario de Atención'`; expect `schedule` to have 2 entries whose `days` are `['Lunes a Jueves', 'Viernes']`; expect each `entry.hours` to be an array of length 2 with the exact split-shift blocks (`['9:00 a 13:00 hrs.', '14:00 a 18:00 hrs.']` for Lunes a Jueves, `['9:00 a 13:00 hrs.', '14:00 a 17:00 hrs.']` for Viernes). Replace the SERVICIOS labels assertion with the new labels: `['Medición en Edificios', 'Medición Industrial', 'Obras y Proyectos', 'Tratamiento de Agua y Desalinización']`. Keep the EMPRESA and `href="#"` assertions. Run → fails.
- [x] 2.2 Update `apps/web/src/lib/config/footer.ts` (GREEN) — add `scheduleTitle: 'Horario de Atención'`; replace the `schedule` array with the two split-shift entries (`hours: [...]` arrays); replace the four `SERVICIOS` link labels with `Medición en Edificios`, `Medición Industrial`, `Obras y Proyectos`, `Tratamiento de Agua y Desalinización` (all still `href: '#'`). Update the file-level JSDoc to reflect the new copy (no longer "mockup verbatim"). Re-run the config test → green.

## 3. Footer component — schedule title from props + multi-`<dd>` rendering

TDD: update the component test first (red), then update the component (green).

- [x] 3.1 Update `apps/web/src/components/__tests__/Footer.test.ts` (RED) — in the schedule describe block: (a) assert the schedule column title `<p>` contains the visible text `HORARIO DE ATENCIÓN` and the `text-primary`, `font-heading`, `uppercase` classes; (b) replace the single-`<dd>` assertions with multi-`<dd>` assertions: the `<dl>` contains `<dt>Lunes a Jueves</dt>` followed by exactly two `<dd>` blocks with texts `9:00 a 13:00 hrs.` and `14:00 a 18:00 hrs.` (in order), and `<dt>Viernes</dt>` followed by `<dd>9:00 a 13:00 hrs.</dd>` and `<dd>14:00 a 17:00 hrs.</dd>`; (c) assert the `<dt>` carries `text-white` + `font-semibold` and every `<dd>` carries `text-muted`. In the columns describe block: update the SERVICIOS link labels to the new four labels. Add a regression assertion: the rendered HTML does NOT contain the literal `HORARIO TÉCNICO`. Run → fails.
- [x] 3.2 Update `apps/web/src/components/__tests__/helpers/footer-test-utils.ts` (if needed) — if the existing helpers don't cover extracting multiple `<dd>` per `<dt>`, add a small `getScheduleEntries(html)` helper that returns `Array<{ dt: string; dd: string[] }>`. Keep the helper file under its line budget.
- [x] 3.3 Update `apps/web/src/components/Footer.astro` (GREEN) — destructure `scheduleTitle` from `Astro.props`; replace the hardcoded `HORARIO TÉCNICO` text node with `{scheduleTitle}` (keep the surrounding `<p>` and its classes — `uppercase` will render "HORARIO DE ATENCIÓN"); rewrite the `<dl>` inner block so each `entry.hours.map((h) => <dd class="... text-muted">{h}</dd>)` renders N `<dd>` after the single `<dt>`. Choose a flex layout that stacks the `<dd>` blocks vertically next to the `<dt>` (e.g. `<div class="flex items-baseline justify-between gap-4"><dt …/> <div class="space-y-1">…<dd/>…</div></div>`), keeping the muted/white tokens. Re-run the component test → green.
- [x] 3.4 Regenerate the snapshot (REFACTOR) — run `pnpm vitest -u` for `apps/web/src/components/__tests__/__snapshots__/Footer.test.ts.snap` only. Verify the snapshot diff contains exactly the expected changes (new schedule rows, new title, new SERVICIOS labels) and nothing else. Commit the snapshot update as part of this group.

## 4. SiteCredits — types, config, component, layout wiring

TDD: tests first for types, config and component; then implement; then wire into `Layout.astro`.

- [x] 4.1 Create `apps/web/src/lib/types/site-credits.ts` with `SiteCreditsProps` — `readonly developerLabel: string`, `readonly developerName: string`, `readonly developerUrl: string`. No `any`.
- [x] 4.2 Create `apps/web/src/lib/types/__tests__/site-credits.test.ts` (RED) — assert the interface shape (a literal with the three fields is assignable to `SiteCreditsProps`; a literal missing any field is NOT).
- [x] 4.3 Create `apps/web/src/lib/config/site-credits.ts` with `SITE_CREDITS_CONTENT: Readonly<SiteCreditsProps>` — `{ developerLabel: 'Diseñado y desarrollado por:', developerName: 'Gabriel Zavando', developerUrl: 'https://gabrielzavando.cl' }`. JSDoc explaining the rationale (client attribution below the footer).
- [x] 4.4 Create `apps/web/src/lib/config/__tests__/site-credits.test.ts` (RED) — assert the three exact values and that `SITE_CREDITS_CONTENT` is `Readonly<SiteCreditsProps>`.
- [x] 4.5 Create `apps/web/src/components/SiteCredits.astro` (GREEN) — dumb component, destructure `developerLabel`, `developerName`, `developerUrl` from `Astro.props`, render a full-width strip (`bg-secondary`, `py-2`, text centered) with the label in a `<span class="text-muted font-body text-xs">` and the name in an `<a class="text-primary hover:text-primary-light no-underline transition-colors duration-200 font-body text-xs" href={developerUrl} target="_blank" rel="noopener noreferrer" aria-label="...">`. No `rounded*`, no `shadow*`, no raw hex.
- [x] 4.6 Create `apps/web/src/components/__tests__/SiteCredits.test.ts` (RED → GREEN) — cover: the strip renders the label text and the anchor with `href="https://gabrielzavando.cl"`, `target="_blank"`, `rel="noopener noreferrer"`, visible text "Gabriel Zavando"; the anchor's class list contains `no-underline` and does NOT contain `underline`/`hover:underline`; the anchor's class list contains `text-primary`, `hover:text-primary-light`, `transition-colors`, `duration-200`; the component source has no raw hex literal; no `rounded*`/`shadow*` classes; the outermost element spans full width (no `container`/`max-w-*` on the outermost); the component source only destructures props and imports types/config (dumb-contract assertion). Use the `experimental_AstroContainer` pattern from `helpers/footer-test-utils.ts` (extract a parallel `site-credits-test-utils.ts` if it helps keep the test file under 400 lines).
- [x] 4.7 Wire `SiteCredits` into `apps/web/src/layouts/Layout.astro` — import `SiteCredits` and `SITE_CREDITS_CONTENT`, render `<SiteCredits {...SITE_CREDITS_CONTENT} />` immediately after `<Footer {...SITE_FOOTER_CONTENT} />` (line ~103). Update `apps/web/src/layouts/__tests__/Layout.test.ts` if it asserts the order/nesting of chrome children (RED: assert `<SiteCredits/>` output follows `<Footer/>` output; GREEN: the wiring already satisfies it).

## 5. Scrollbar — `@layer base` rules in `globals.css`

TDD: a new scrollbar test first (red), then the CSS (green).

- [x] 5.1 Create `apps/web/src/styles/__tests__/scrollbar.test.ts` (RED) — read `apps/web/src/styles/globals.css` (same `readFileSync` pattern as `tokens.test.ts`); assert: (a) a `scrollbar-color` declaration on `html` (or `*`) whose thumb value is `var(--color-secondary-dark)` and track is `transparent`; (b) an `::-webkit-scrollbar-thumb` rule with `background-color: var(--color-secondary-dark)`; (c) an `::-webkit-scrollbar-thumb:hover` rule with `background-color: var(--color-secondary-light)`; (d) `::-webkit-scrollbar-track` declares `background: transparent` (or `background-color: transparent`); (e) `::-webkit-scrollbar-thumb` declares `border-radius: 0`; (f) `::-webkit-scrollbar` declares `width` and `height` between 8 and 16px; (g) the file does NOT contain the raw literal `#16202E` (regression guard for the no-raw-hex rule). Run → fails.
- [x] 5.2 Update `apps/web/src/styles/globals.css` (GREEN) — inside `@layer base`, alongside the existing `html`/`body` rules, add: `scrollbar-color: var(--color-secondary-dark) transparent;` on `html`; a `::-webkit-scrollbar { width: 12px; height: 12px; }` block; `::-webkit-scrollbar-track { background: transparent; }`; `::-webkit-scrollbar-thumb { background-color: var(--color-secondary-dark); border-radius: 0; }`; `::-webkit-scrollbar-thumb:hover { background-color: var(--color-secondary-light); }`. No raw hex. Re-run the test → green.
- [x] 5.3 Verify `apps/admin/src/styles/globals.css` is untouched (the scrollbar rules live in `apps/web` only; the admin `sync.test.ts` validates `@theme` parity and `@layer base` rules do not participate).

## 6. Verification — full suite, lint, typecheck, build

- [x] 6.1 Run the full `apps/web` Vitest suite (`pnpm --filter web test` or the project's equivalent) — all green including the regenerated footer snapshot and the new SiteCredits + scrollbar tests.
- [x] 6.2 Run lint + typecheck for `apps/web` (`pnpm --filter web lint`, `pnpm --filter web exec tsc --noEmit` or the project's equivalent) — no errors.
- [x] 6.3 Run the `apps/admin` design-token `sync.test.ts` — still green (no `@theme` change, no admin scrollbar mirror).
- [x] 6.4 Run a production build of `apps/web` (`pnpm --filter web build`) — succeeds; manually inspect one generated HTML page (e.g. `dist/index.html`) for: the new schedule title `HORARIO DE ATENCIÓN`, two split-shift `<dd>` rows per day range, the four new SERVICIOS labels, the credits strip with the external link to `https://gabrielzavando.cl`, and the scrollbar rules compiled into the bundled CSS.
- [x] 6.5 Update `openspec/changes/footer-credits-scrollbar/tasks.md` — check every task above as done; the change is ready for `/verify` then `/archive`.
