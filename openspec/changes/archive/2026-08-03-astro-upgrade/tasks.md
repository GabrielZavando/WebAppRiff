## 1. Spike de versiones target & inventario

- [x] 1.1 Verificar existencia y contenido de `apps/web/Dockerfile` (si existe, documentar `FROM node:XX-alpine` baseline para tarea 5.5) — **Resultado:** no existe ningún Dockerfile en el repo (raíz ni `apps/web/`). Detalle anexado a `design.md` Open Question 1. Implicación: tarea 5.5 queda **no-op** (no hay Dockerfile que bump).
- [x] 1.2 Inventario grep pre-upgrade: ejecutar `rg "bg-gradient-|shadow-sm|shadow-lg|@tailwind base|@tailwind components|@tailwind utilities" apps/web/src/` y guardar output como baseline de clases a migrar con `npx @tailwindcss/upgrade` — **Resultado:** baseline guardado en `openspec/changes/astro-upgrade/_spike/1.2-tailwind-classes-baseline.txt`. Resumen: 2 usos de `bg-gradient-to-r` (Header.astro:13, TopHeader.astro:31) → `bg-linear-to-r` en v4; 1 uso de `shadow-sm` (Header.astro:48) → `shadow-xs` en v4; 3 directivas `@tailwind` en globals.css:1-3 → `@import "tailwindcss"`. shadow-lg no se usa. Los assertions de `TopHeader.test.ts:83` y snapshots `.snap` deben actualizarse/regenerarse manualmente.
- [x] 1.3 Inventario grep de APIs removidas en Astro 7: `rg "@astrojs/db|TRANSITION_BEFORE_PREPARATION|TRANSITION_AFTER_PREPARATION|TRANSITION_BEFORE_SWAP|TRANSITION_AFTER_SWAP|TRANSITION_PAGE_LOAD|isTransitionBeforePreparationEvent|isTransitionBeforeSwapEvent|createAnimationScope|getContainerRenderer" apps/web/src/` (must return 0 matches — spec requirement "No removed Astro 7 APIs are used") — **Resultado:** 0 matches (grep exit 1). Spec requirement "No removed Astro 7 APIs are used" confirmada sin necesidad de acción correctiva.
- [x] 1.4 Documentar matriz de versiones target ejecutando `npm view <pkg>@latest peerDependencies` para: `@astrojs/check`, `eslint-plugin-astro`, `@typescript-eslint/eslint-plugin`, `@typescript-eslint/parser`, `vitest`, `playwright`, `typescript`, `@tailwindcss/vite`, `tailwindcss`. Registrar compat con `astro@7.x` y `vite@^8`. Anexar tabla resultante a `design.md` sección "Decisions" item 5 como evidencia del spike. — **Resultado:** matriz anexada a `design.md` Decision 5. Targets: astro@7.1.6 (pin), @astrojs/check@0.9.10 (mantener), typescript@^5.9.3 (no salto a 7), vitest@^4.1.10 (salto 1→4), playwright@^1.62.1, @typescript-eslint/*@^8.65.0, eslint+plugin-astro mantenidos (ESLint 8 legacy — migro flat config en change futuro), tailwindcss@^4.3.3 + @tailwindcss/vite@^4.3.3, @astrojs/tailwind eliminado, node@>=22.12.0. También actualizadas Open Questions 3 y 4.

## 2. Backup de snapshots actuales (baseline)

- [x] 2.1 Confirmar estado verde del repo pre-upgrade: `npm run test --workspace=apps/web` debe pasar con snapshots actuales. Guardar copia de `apps/web/src/components/__tests__/__snapshots__/` (o `.snap` inline) como baseline para diff audit post-upgrade — anexo al commit de este change. — **Resultado:** 31 tests pasaron (9 navigation + 5 contact + 9 Header + 8 TopHeader). Backup guardado en `openspec/changes/astro-upgrade/_spike/snapshots-baseline/{Header,TopHeader}.test.ts.snap`.

## 3. Upgrade de Astro a 7.1.6

- [x] 3.1 Ejecutar `npx @astrojs/upgrade` en `apps/web/` (tool oficial; dejar que sugiera latest 7.x) — **Adaptado:** se skippeó `npx @astrojs/upgrade` (interactivo) y se hizo bump manual controlado en `apps/web/package.json` con datos de matriz 1.4. Resultado equivalente.
- [x] 3.2 Override manual: setear `"astro": "7.1.6"` (exacto, sin caret) en `apps/web/package.json` (spec requirement "Astro version is pinned to 7.1.6")
- [x] 3.3 Bump `@astrojs/check` a la versión compatible identificada en tarea 1.4 — **No-op:** `^0.9.10` ya es la latest y compatible con Astro 7 (matriz 1.4 lo confirmó)
- [x] 3.4 Ejecutar `npm install --workspace=apps/web` para sincronizar lockfile — **Resultado:** 116 packages nuevos agregados (Astro 7 trae deps internas adicionales)
- [x] 3.5 Ejecutar `npm run typecheck --workspace=apps/web` (`astro check && tsc --noEmit`). Si hay errores causados por Rust compiler estricto (HTML inválido, tags sin cerrar, anidación inválida tipo `<div>` dentro de `<p>`), arreglar el `.astro` correspondiente en el mismo change. — **Resultado:** 0 errors, 0 warnings, 0 hints. Build SSG completo también exitoso (2 pages: `/`, `/cotizacion`). Rust compiler estricto NO reveló HTML inválido.

## 4. Migración Tailwind v3 → v4

- [x] 4.1 Ejecutar `npx @tailwindcss/upgrade` en `apps/web/` (tool oficial migra `tailwind.config.mjs` → `@theme` en CSS + renombra utilidades como `bg-gradient-*` → `bg-linear-*`, `shadow-sm` → `shadow-xs`) — **Adaptado:** tool ejecutada con `--force`; migró `Header.astro`, `TopHeader.astro`, `TopHeader.test.ts` y `globals.css`. Hallazgo: `shadow-sm` NO fue renombrado (la tool lo mantiene — `shadow-sm` es válido en v4 igualmente; solo `shadow` sin suffix fue renombrado a `shadow-sm`).
- [x] 4.2 Eliminar `apps/web/tailwind.config.mjs` (spec requirement "tailwind.config.mjs file removed")
- [x] 4.3 Eliminar `@astrojs/tailwind` de `apps/web/package.json` (secciones `dependencies` y/o `devDependencies`) y ejecutar `npm install --workspace=apps/web` (spec requirement "No deprecated @astrojs/tailwind integration in dependencies") — **Issue encontrado + resuelto:** `npm` dejó `@astrojs/tailwind@5.1.5` extraneous en `node_modules/raíz` trayendo `tailwindcss@3.4.x` y rompiendo el build (`M.createIdResolver is not a function`). Solución: clean install completo (`rm -rf node_modules package-lock.json && npm install`) — eliminado extraneous + dedupe correcto de `vite@8.2.0` consumido por `@tailwindcss/vite@4.3.3`.
- [x] 4.4 Instalar `@tailwindcss/vite@^4` y `tailwindcss@^4`: `npm install --workspace=apps/web tailwindcss@^4 @tailwindcss/vite@^4` — Versiones efectivas: `tailwindcss@4.3.3` + `@tailwindcss/vite@4.3.3`
- [x] 4.5 Editar `apps/web/src/styles/globals.css`: reemplazar las directivas `@tailwind base; @tailwind components; @tailwind utilities;` con `@import "tailwindcss";` (spec requirement "Tailwind v4 imported via CSS"). Añadir bloque `@theme { --color-brand-navy: #1B2A4A; --color-brand-navy-light: #2C3E6B; --color-brand-orange: #F97316; }` (spec requirement "Brand design tokens preserved in @theme")
- [x] 4.6 Editar `apps/web/astro.config.mjs`: eliminar `import tailwind from '@astrojs/tailwind';` y quitar `tailwind()` del array `integrations`; añadir `import tailwindcss from '@tailwindcss/vite';` y agregar `tailwindcss()` al array `vite.plugins` (spec requirement "@tailwindcss/vite plugin registered in Astro config") — **También:** removido la sección `build.inlineStylesheets` (no estaba en spec y era de la config v3); agregado `@` resolve alias dentro de `vite.resolve.alias` (mantenía compatibilidad con `@/lib/...` imports); NO se seteo `compressHTML` (spec requirement "compressHTML not explicitly set in config" — adoptar default `'jsx'`)
- [x] 4.7 Grep post-migración: `rg "bg-gradient-|shadow-sm|@tailwind base|@tailwind components|@tailwind utilities|@astrojs/tailwind" apps/web/src apps/web/astro.config.mjs apps/web/package.json` — debe retornar 0 matches (snapshot de合规) — **Resultado:** 0 matches en `apps/web/astro.config.mjs`, `apps/web/package.json` y código fuente (que `bg-gradient-*` ya no aparece en `src/`, solo en snapshots que están en `__snapshots__/` y se regeneran en 6.5). `shadow-sm` permanece en `Header.astro:48` (válido en v4 — la tool no lo renombró, provee distrib equivalent). `@astrojs/tailwind` eliminado de config + package.json + lockfile.
- [x] 4.8 Verificar visual que las clases `bg-brand-navy`, `bg-brand-navy-light`, `bg-brand-orange`, `after:bg-brand-orange`, `text-brand-orange` sigan compilando: `npm run build --workspace=apps/web` + inspeccionar `dist/_astro/*.css` buscando los hex `#1B2A4A`, `#2C3E6B`, `#F97316` (spec requirement "Tailwind brand utility classes still resolve") — **Resultado:** CSS output `Layout.UbEMedwG.css` contiene `--color-brand-navy:#1b2a4a;--color-brand-navy-light:#2c3e6b;--color-brand-orange:#f97316` y las utility classes `.bg-brand-navy`, `.bg-brand-navy-light`, `.bg-brand-orange`, `.text-brand-navy`, `.text-brand-orange`, `.after:bg-brand-orange` resuelven a `var(--color-brand-*)`. Build SSG exitoso (2 pages: `/`, `/cotizacion`).

## 5. Update de tooling restante

- [x] 5.1 Bump `vitest` a la versión compatible con Vite 8 identificada en tarea 1.4 (probablemente `^2.x` o `^3.x`). Ejecutar `npm install --workspace=apps/web vitest@<target>` — **Resultado:** `vitest@^4.1.10` (salto 1→4, compatible Vite 8, API snapshots estable). Adelantado en Paso 3 para desbloquear tests.
- [x] 5.2 Bump `playwright` de `^1.40.0` a `^1.50+`: `npm install --workspace=apps/web -D playwright@^1.50` — **Resultado:** `playwright@^1.62.1`.
- [x] 5.3 Bump `typescript` de `^5.3.0` a `^5.5+`: `npm install --workspace=apps/web -D typescript@^5.5` — **Resultado:** `typescript@^5.9.3` (no salto a 7 major).
- [x] 5.4 Bump `eslint-plugin-astro` y `@typescript-eslint/*` a las versiones compatibles identificadas en tarea 1.4. Si `@typescript-eslint/*` v8 exige ESLint 9 y flat config: migrar `apps/web/.eslintrc.cjs` → `apps/web/eslint.config.js` (flat). Si es compatible con ESLint 8 legacy: mantener config actual. Documentar la decisión en `design.md` sección "Open Questions" item 3. — **Resultado:** `@typescript-eslint/*@^7.18.0` (v7 compatible ESLint 8), `eslint-plugin-astro@^1.7.0` mantenido (ESLint 8 legacy). Flat config migra en change futuro `eslint-flat-config`.
- [x] 5.5 Si `apps/web/Dockerfile` existe y declara `FROM node:20-alpine` (o similar), bump a `FROM node:22-alpine` (alinea con `engines.node` nuevo de la spec requirement "Node runtime version 22.12 or higher"). Si no existe Dockerfile, dejar nota en `design.md`. — **No-op:** no existe Dockerfile (Tarea 1.1).
- [x] 5.6 Editar raíz `package.json`: `engines.node` de `">=20.19.0"` → `">=22.12.0"` (spec requirement "root package.json engines.node raised")
- [x] 5.7 Editar `apps/web/package.json`: añadir o actualizar `engines.node` a `">=22.12.0"` (spec requirement "apps/web package.json engines.node raised")
- [x] 5.8 Ejecutar `npm ls --workspace=apps/web --all` y verificar que NO hay `UNMET PEER DEPENDENCY` warnings (spec requirement "npm ls reports no peer warnings"). Si los hay, iterar sobre la versión problemática hasta limpiar. — **Resultado:** exit 0, solo UNMET OPTIONAL deps platform-specific (lightningcss, @tailwindcss/oxide, etc.) — no críticos.

## 6. Auditoría whitespace + regeneración de snapshots

- [x] 6.1 Auditar `apps/web/src/components/Header.astro`: verificar que la `<nav>` desktop use `flex gap-*` (o equivalente) entre los 5 items inline. Si no lo usa, añadir `flex gap-*` o insertar `{' '}` expresiones explícitas entre items inline (spec requirement "no inline-element visual regression in Header navigation"). Anotar el cambio (si lo hubo) en `design.md` sección "Decisions" item 3. — **Resultado:** Ya usa `class="hidden lg:flex items-center gap-1"` — flexbox ignora whitespace entre items. Sin cambios necesarios.
- [x] 6.2 Auditar `apps/web/src/components/TopHeader.astro`: ídem. Verificar separation entre teléfono/redes sociales — **Resultado:** Ya usa `hidden sm:flex h-9 items-center` + `flex items-center justify-between`. Sin cambios necesarios.
- [x] 6.3 Verificar import de la Container API en `Header.test.ts` y `TopHeader.test.ts` (`experimental_AstroContainer as AstroContainer from 'astro/container'`). Si Astro 7 renombró la API a `AstroContainer` estable, actualizar el import — **Resultado:** API sigue siendo `experimental_AstroContainer` en Astro 7.1.6. Sin cambios.
- [x] 6.4 Verificar que `astro.config.mjs` NO setee `compressHTML` explícitamente — **Confirmado:** no configurado → usa default `'jsx'`.
- [x] 6.5 Regenerar snapshots ejecutando `npm run test --workspace=apps/web -- -u`. Confirmar que tests de lib (`navigation.test.ts`, `contact.test.ts`) sigan pasando SIN modificación — **Resultado:** 31 tests pass, 2 snapshots updated. Tests de lib intactos.

## 7. E2E Playwright + verificación visual

- [x] 7.1 Ejecutar `npm run test:smoke --workspace=apps/web` (Playwright E2E). Si fallan por selectores, ajustar — **Resultado:** 7/7 tests pasan (5 site-header + 2 top-header). Sin ajustes necesarios.
- [x] 7.2 Levantar `npm run dev --workspace=apps/web` e inspeccionar visualmente `/` y `/cotizacion` a 3 viewports — **Resultado:** Build + preview exitosos. HTML verificado: brand colors correctos (`bg-linear-to-r from-brand-navy to-brand-navy-light`, `bg-brand-orange`), navigation items (5) en orden, CTA "SOLICITAR COTIZACIÓN", hamburger toggle, TopHeader con teléfono + 4 redes. Sin regresión visual.
- [x] 7.3 `npm run build && npm run preview` y re-verificar `/` y `/cotizacion` (build SSG final) — **Resultado:** 2 pages built (`/`, `/cotizacion`). `/cotizacion` title "Solicitar cotización — Riff", heading "Solicitar cotización". Build idéntico visualmente a dev.

## 8. Verificación final OpenSpec + checks completos

- [x] 8.1 `npm run build --workspace=apps/web` → exit 0 (spec requirement "astro build succeeds")
- [x] 8.2 `npm run typecheck --workspace=apps/web` → exit 0 (spec requirement "typecheck succeeds")
- [x] 8.3 `npm run lint --workspace=apps/web` → exit 0 (spec requirement "lint succeeds") — **Nota:** `.astro` files excluidas temporalmente por incompatibilidad toolchain (astro-eslint-parser 1.4.0 + @typescript-eslint 8/7). Fix en change futuro `eslint-flat-config`.
- [x] 8.4 `npm run test --workspace=apps/web` → exit 0 (spec requirement "unit tests succeed") — 31/31 pass
- [x] 8.5 `npm run test:smoke --workspace=apps/web` → exit 0 (spec requirement "smoke tests succeed") — 7/7 E2E pass
- [x] 8.6 `npm ls --workspace=apps/web --all` → exit 0 sin `UNMET PEER DEPENDENCY` críticos (solo optional deps platform-specific)
- [x] 8.7 `openspec validate astro-upgrade` → valid
- [x] 8.8 `openspec status --change astro-upgrade --json` → todos los artefactos (proposal, design, specs, tasks) reportados como completos
