# Tasks Template — OpenSpec Change

> This file is auto-generated by OpenSpec when running `/plan-change`.
> **DO NOT EDIT manually** without updating OpenSpec artifacts first.

## Change Summary

[One-paragraph description of the change]

---

## Mandatory Steps

> Injected by `plan-change` from `docs/openspec-tasks-mandatory-steps.md` (single
> source of truth, read at generation time). This checklist is **mandatory, not
> suggested**, and applies to every implementation task executed via `/apply`.

**Pre-implementation**

- [ ] Active branch follows the project's current convention (e.g. `feature/*`,
  `fix/*`); never work directly on the main branch
- [ ] Clean git state: no uncommitted (or staged) changes before starting

**During implementation**

- [ ] New test fails before implementing (RED): write the `SC-NNN` scenario test
  and watch it fail before writing production code
- [ ] Module unit tests: run the touched module's tests while iterating
  (RED-GREEN-REFACTOR), not only at the end

**Post-implementation**

- [ ] Run `verify`: change verification produces persistent evidence
  (`openspec/state/verify-results.json`)
- [ ] Run `adversarial-review`: the adversarial audit produces a persistent
  verdict (`openspec/state/adversarial-result.json`)

---

## Task Format

Each task follows this structure:

```markdown
### Task N: [Short description]

- [ ] Subtask 1 (use `[ ]` for pending, `[x]` for completed)
- [ ] Subtask 2
- Priority: [High/Medium/Low]
- Layer: [Backend (API/Data/Domain) / Frontend (UI/State/API)]
- Estimated: [X hours/days]
```

---

## Example: User Registration (SCRUM-42)

### Task 1: Create User entity and migration ✅ COMPLETED

- [x] Write User entity test
- [x] Implement User entity with fields: id, email, password_hash, status, created_at, updated_at
- [x] Create database migration for users table
- [x] Update `docs/data-model/data-model.md` with User entity
- Priority: High
- Layer: Backend (Data)
- Estimated: 2 hours

### Task 2: Implement POST /api/v1/auth/register endpoint

- [ ] Write integration test for register endpoint
- [ ] Implement request validation (email format, password strength)
- [ ] Implement password hashing with bcrypt (cost factor 12)
- [ ] Implement user creation service
- [ ] Return 201 with user data (excluding password_hash)
- [ ] Return 422 for validation errors
- [ ] Return 409 for duplicate email
- [ ] Update `docs/api/api-spec.yml` with new endpoint
- Priority: High
- Layer: Backend (API)
- Estimated: 4 hours

### Task 3: Create verification email service

- [ ] Write unit test for email service
- [ ] Implement verification token generation (32 bytes, base64url)
- [ ] Implement token storage (hash in DB, not plaintext)
- [ ] Implement email template (verification link)
- [ ] Integrate with Resend API
- [ ] Set token expiry (24 hours)
- Priority: High
- Layer: Backend (Domain)
- Estimated: 3 hours

### Task 4: Implement email verification endpoint

- [ ] Write integration test for `/verify-email`
- [ ] Implement GET `/api/v1/auth/verify-email?token=xxx`
- [ ] Validate token exists and not expired
- [ ] Update user status to "active"
- [ ] Invalidate token after use
- [ ] Return appropriate errors for expired/invalid tokens
- Priority: High
- Layer: Backend (API)
- Estimated: 2 hours

### Task 5: Create RegistrationForm component

- [ ] Write component test for RegistrationForm
- [ ] Implement form with: email, password, confirmPassword, name fields
- [ ] Implement client-side validation (real-time)
- [ ] Implement loading, error and success states
- [ ] Connect to POST `/api/v1/auth/register`
- [ ] Redirect to email verification notice on success
- Priority: High
- Layer: Frontend (UI)
- Estimated: 3 hours

### Task 6: Add password strength indicator

- [ ] Write unit test for password strength utility
- [ ] Implement password strength calculation (8+ chars, 1 number, 1 uppercase)
- [ ] Visual indicator component (weak/medium/strong)
- [ ] Integrate into RegistrationForm
- Priority: Medium
- Layer: Frontend (UI)
- Estimated: 2 hours

### Task 7: Implement rate limiting

- [ ] Write unit test for rate limiter
- [ ] Implement in-memory rate limiter (5 attempts/10 min per IP)
- [ ] Add Redis fallback if available
- [ ] Add rate limit headers to responses
- Priority: Medium
- Layer: Backend (Infrastructure)
- Estimated: 2 hours

---

## Guidelines

1. **One task at a time:** Only the first pending task is implemented per `/apply` cycle
2. **TDD mandatory:** Write failing test before implementing
3. **Mark complete:** Update `[ ]` → `[x]` after implementation and passing tests
4. **Update docs:** If the task modifies API or data model, update the docs immediately after code
5. **Priority order:** High → Medium → Low. Within same priority, order listed is execution order

## TDD Failure Report Example

When a task fails TDD for 3 consecutive attempts (see the **TDD Failure Protocol** in `.opencode/commands/apply.md`), the agent emits this report and **stops** — the task is NOT marked complete and no further task is started:

```
TDD Failure Report
Task: 3.3 Implement token storage (hash in DB, not plaintext)
Attempt: 3
Error: TokenService.hashToken() returns plaintext token — unit test `hashes token before insert` expects sha512 digest, received raw base64url string
Suggested investigation: verify TokenService imports the crypto hashing helper instead of the base64url encoder; check that `hashToken` is not aliased to `encodeToken` in the DI container
```

After emitting the report the agent waits for explicit user instruction. A user instruction to retry resets the attempt counter and the protocol applies again.

## Metadata

| Field | Value |
|-------|-------|
| Ticket | SCRUM-42 |
| Created | 2024-01-15 |
| Total Tasks | 7 |
| Completed | 1 |
| Remaining | 6 |
| Estimated Total | 18 hours |